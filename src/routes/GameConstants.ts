// Export individual constants
export const BLOCK_SIZE = 1;
export const RENDER_DISTANCE = 2;
export const CHUNK_SIZE = 8;
export const BREAK_DISTANCE = 5;
export const INVENTORY_SIZE = 9;
export const HOTBAR_SIZE = 9;
export const DAY_LENGTH = 1200; // in seconds
export const MOVEMENT_SPEED = 5;
export const GRAVITY = 9.81;
export const JUMP_FORCE = 5;
export const PLAYER_HEIGHT = 1.8;
export const PLAYER_RADIUS = 0.4;
export const GROUND_OFFSET = 0.1;

// Also export as a group if needed
export const GameConstants = {
    BLOCK_SIZE,
    RENDER_DISTANCE,
    CHUNK_SIZE,
    BREAK_DISTANCE,
    INVENTORY_SIZE,
    HOTBAR_SIZE,
    DAY_LENGTH,
    MOVEMENT_SPEED,
    GRAVITY,
    JUMP_FORCE,
    PLAYER_HEIGHT,
    PLAYER_RADIUS,
    GROUND_OFFSET
}; 